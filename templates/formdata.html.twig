{% extends 'partials/base.html.twig' %}
{% block content %}
<section id="about">
    <div class="row">
        <div class="col-md-9 main-col">
            <h2>{{ form.message }}</h2>
            {{ content }}

            {% include "forms/data.html.twig" %} <br />
            <a href="{{ base_url_absolute }}">Start</a>.
        </div>
    </div>
</section>
{% endblock %}
